services:
  local-secrets-manager:
    build: .
    container_name: local-secrets-manager
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 5000
      APP_DATA_DIR: /app/data
    restart: "no"
